import styled from '@emotion/styled';

export const Wrapper = styled.div`
  position: relative;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: fit-content;

  p {
    font-size: 40px;
  }
`;
